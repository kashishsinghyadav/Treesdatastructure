class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        s=""
        if str1+str2 != str2+str1:
            return s
        n1=len(str1)
        n2=len(str2)
        gcd_val = math.gcd(n1,n2)
        return str1[:gcd_val]
